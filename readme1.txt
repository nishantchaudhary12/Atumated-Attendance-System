Save all the photos in the photos folder. Make sure the name of the photo is firstname_lastname of that student.
Then run the application in the terminal, use command:
python app_attendace.py
Then wait for the camera to scan all the faces, once done press key 'q' to stop.
